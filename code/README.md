# Code

## Scripts

- `parse_onshape.py` - Main parser (fork of onshape-cad-parser)
- `export_step.py` - STEP export at rollback states
- `validate.py` - Quality control checks

## Dependencies

- Python 3.8+
- onshape-client
- OCC (Open CASCADE) for STEP validation
